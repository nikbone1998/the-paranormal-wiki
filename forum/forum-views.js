(()=>{'use strict';
const C=window.ForumCore,{db,state,$,esc,rel,clip,displayName,urlFor,activeTab,crumbs,pageHead,newDiscussionButton,pager,profiles,threadMetadata,hydrateThreads,threadRow,requireProfile}=C;
async function latestRows(limit=12,offset=0){const{data,error}=await db.from('forum_threads').select('id,title,category_id,author_id,created_at,last_post_at,is_pinned,is_locked,moderation_status').eq('moderation_status','visible').order('last_post_at',{ascending:false}).range(offset,offset+limit-1);if(error)throw error;return hydrateThreads(data||[])}
async function boardCards(){const summaries=await Promise.all(state.categories.map(async c=>{const[countRes,lastRes]=await Promise.all([db.from('forum_threads').select('*',{count:'exact',head:true}).eq('category_id',c.id).eq('moderation_status','visible'),db.from('forum_threads').select('id,title,author_id,last_post_at').eq('category_id',c.id).eq('moderation_status','visible').order('last_post_at',{ascending:false}).limit(1)]);if(countRes.error)throw countRes.error;if(lastRes.error)throw lastRes.error;return{category:c,count:countRes.count||0,last:lastRes.data?.[0]||null}}));const pm=await profiles(summaries.map(x=>x.last?.author_id));return summaries.map(({category:c,count,last})=>`<article class="board-card"><div><div class="board-name"><a data-route href="${urlFor({category:c.slug})}">${esc(c.name)}</a>${c.is_locked?' <span class="status locked">LOCKED</span>':''}</div><div class="board-desc">${esc(c.description)}</div><div class="board-count">${count} ${count===1?'discussion':'discussions'}</div></div><div class="board-latest"><div class="board-latest-label">LATEST DISCUSSION</div>${last?`<a class="board-latest-title" data-route href="${urlFor({thread:last.id})}">${esc(last.title)}</a><div class="board-latest-meta">by ${esc(displayName(pm.get(last.author_id)))} · ${esc(rel(last.last_post_at))}</div>`:'<div class="board-latest-meta">No discussions yet.</div>'}</div></article>`).join('')}
async function renderHome(){activeTab('home');crumbs([{label:'Forum'}]);$('#forumContent').innerHTML=pageHead('COMMUNITY INDEX','Forum Home','Latest activity first, then the complete board directory.',newDiscussionButton())+'<div class="hero-grid"><section class="panel"><div class="panel-title">ACTIVE DISCUSSIONS <a data-route href="/forum/?view=latest">VIEW ALL</a></div><div id="homeLatest" class="thread-list"><div class="loading-state">LOADING ACTIVITY…</div></div></section><section class="panel"><div class="panel-title">START HERE</div><div class="panel-body"><p style="margin:0;color:#8d9a86;font:12px/1.55 var(--sans)">Browse the boards, search past discussions, or start a new thread. Forum posts are community submissions—not automatically verified archive evidence.</p></div></section></div><section class="panel" style="margin-top:10px"><div class="panel-title">BOARD DIRECTORY <a data-route href="/forum/?view=boards">OPEN BOARDS</a></div><div id="homeBoards" class="board-directory"><div class="loading-state">LOADING BOARDS…</div></div></section>';const rows=await latestRows(10);$('#homeLatest').innerHTML=rows.length?rows.map(threadRow).join(''):'<div class="empty-state">No discussions yet.</div>';$('#homeBoards').innerHTML=await boardCards()}
async function renderBoards(){activeTab('boards');crumbs([{label:'Forum',href:'/forum/'},{label:'Boards'}]);$('#forumContent').innerHTML=pageHead('DIRECTORY','Boards','Nine paranormal discussion areas, organized for quick scanning.',newDiscussionButton())+'<section class="panel"><div class="panel-title">ALL BOARDS</div><div id="boardsAll" class="board-directory"><div class="loading-state">LOADING BOARDS…</div></div></section>';$('#boardsAll').innerHTML=await boardCards()}
async function renderLatest(page=1){activeTab('latest');crumbs([{label:'Forum',href:'/forum/'},{label:'Latest'}]);page=Math.max(1,Number(page)||1);const from=(page-1)*C.BOARD_PAGE_SIZE,countRes=await db.from('forum_threads').select('*',{count:'exact',head:true}).eq('moderation_status','visible');if(countRes.error)throw countRes.error;const rows=await latestRows(C.BOARD_PAGE_SIZE,from),pages=Math.max(1,Math.ceil((countRes.count||0)/C.BOARD_PAGE_SIZE));$('#forumContent').innerHTML=pageHead('ACTIVITY','Latest Discussions','All visible discussions ordered by most recent activity.',newDiscussionButton())+`<section class="panel"><div class="panel-title">LATEST ACTIVITY</div><div class="thread-list">${rows.length?rows.map(threadRow).join(''):'<div class="empty-state">No discussions yet.</div>'}</div>${pager(page,pages,countRes.count||0,'latestPage')}</section>`}
async function renderCategory(slug,page=1){const c=state.categories.find(x=>x.slug===slug);if(!c)return C.renderNotFound('Board not found.');activeTab('boards');crumbs([{label:'Forum',href:'/forum/'},{label:'Boards',href:'/forum/?view=boards'},{label:c.name}]);page=Math.max(1,Number(page)||1);const from=(page-1)*C.BOARD_PAGE_SIZE,{data,error,count}=await db.from('forum_threads').select('id,title,category_id,author_id,created_at,last_post_at,is_pinned,is_locked,moderation_status',{count:'exact'}).eq('category_id',c.id).eq('moderation_status','visible').order('is_pinned',{ascending:false}).order('last_post_at',{ascending:false}).range(from,from+C.BOARD_PAGE_SIZE-1);if(error)throw error;const rows=await hydrateThreads(data||[]),pages=Math.max(1,Math.ceil((count||0)/C.BOARD_PAGE_SIZE));$('#forumContent').innerHTML=pageHead('BOARD',c.name,c.description,newDiscussionButton())+`<section class="panel"><div class="panel-title">DISCUSSIONS</div><div class="thread-list">${rows.length?rows.map(threadRow).join(''):'<div class="empty-state">No discussions yet.</div>'}</div>${pager(page,pages,count||0,'page')}</section>`}
function renderSearch(params){activeTab('search');crumbs([{label:'Forum',href:'/forum/'},{label:'Search'}]);$('#forumContent').innerHTML=pageHead('ARCHIVE QUERY','Search Forum','Search thread titles and post text, optionally by board.')+`<section class="panel"><form id="searchForm" class="search-form"><input id="searchInput" aria-label="Search forum" maxlength="120" placeholder="Search discussions…" value="${esc(params.get('q')||'')}"><select id="searchScope"><option value="all">All forum</option><option value="titles">Thread titles</option><option value="posts">Post text</option></select><select id="searchBoard"><option value="">All boards</option>${state.categories.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select><button class="bbs-btn primary">SEARCH</button></form><div id="searchResults" class="search-results"><div class="empty-state">Enter at least two characters to search the community archive.</div></div></section>`;$('#searchScope').value=params.get('scope')||'all';$('#searchBoard').value=params.get('board')||'';if((params.get('q')||'').length>=2)runSearch(false)}
async function runSearch(push=true){const q=$('#searchInput')?.value.trim()||'',scope=$('#searchScope')?.value||'all',board=$('#searchBoard')?.value||'';if(q.length<2){$('#searchResults').innerHTML='<div class="empty-state">Enter at least two characters.</div>';return}if(push){const obj={view:'search',q,scope};if(board)obj.board=board;history.pushState({},'',urlFor(obj))}$('#searchResults').innerHTML='<div class="loading-state">SEARCHING…</div>';const pattern=`%${q.replaceAll('%','\\%').replaceAll('_','\\_')}%`;let threads=[],posts=[];if(scope!=='posts'){let query=db.from('forum_threads').select('id,title,category_id,author_id,last_post_at').eq('moderation_status','visible').ilike('title',pattern).order('last_post_at',{ascending:false}).limit(30);if(board)query=query.eq('category_id',Number(board));const r=await query;if(r.error)throw r.error;threads=r.data||[]}if(scope!=='titles'){const r=await db.from('forum_posts').select('id,thread_id,body,author_id,created_at').eq('moderation_status','visible').ilike('body',pattern).order('created_at',{ascending:false}).limit(40);if(r.error)throw r.error;posts=r.data||[]}const threadIds=[...new Set([...threads.map(x=>x.id),...posts.map(x=>x.thread_id)])];let threadMap=new Map();if(threadIds.length){const{data,error}=await db.from('forum_threads').select('id,title,category_id,author_id,last_post_at,moderation_status').in('id',threadIds).eq('moderation_status','visible');if(error)throw error;threadMap=new Map((data||[]).map(x=>[x.id,x]))}if(board)posts=posts.filter(p=>String(threadMap.get(p.thread_id)?.category_id)===String(board));const postProfiles=await profiles(posts.map(x=>x.author_id)),catMap=new Map(state.categories.map(c=>[String(c.id),c])),searchMeta=await threadMetadata(threadIds);let html='';if(threads.length){html+='<div class="panel-title">THREAD TITLES</div>'+threads.map(t=>{const m=searchMeta.get(t.id)||{};return`<article class="search-result"><h3><a data-route href="${urlFor({thread:t.id})}">${esc(t.title)}</a></h3><div class="search-meta">${esc(catMap.get(String(t.category_id))?.name||'Forum')} · ${m.replies||0} replies · ${m.posts||0} posts · last activity ${esc(rel(t.last_post_at))}</div></article>`}).join('')}if(posts.length){html+='<div class="panel-title">POST TEXT</div>'+posts.map(p=>{const t=threadMap.get(p.thread_id),m=searchMeta.get(p.thread_id)||{};return`<article class="search-result"><h3><a data-route href="${urlFor({thread:p.thread_id})}#post-${p.id}">${esc(t?.title||'Discussion')}</a></h3><div class="search-excerpt">${esc(clip(p.body))}</div><div class="search-meta">${esc(catMap.get(String(t?.category_id))?.name||'Forum')} · ${esc(displayName(postProfiles.get(p.author_id)))} · ${m.replies||0} replies · ${m.posts||0} posts · ${esc(rel(p.created_at))}</div></article>`}).join('')}$('#searchResults').innerHTML=html||'<div class="empty-state">No matching discussions or posts.</div>'}
async function renderBookmarks(){activeTab('bookmarks');crumbs([{label:'Forum',href:'/forum/'},{label:'Bookmarks'}]);if(!state.session||!state.profile){$('#forumContent').innerHTML=pageHead('ACCOUNT','Bookmarks','Sign in to view your saved discussions.')+'<section class="panel"><div class="empty-state"><button class="bbs-btn primary" data-auth-open="signin">SIGN IN</button></div></section>';return}const{data,error}=await db.from('forum_bookmarks').select('thread_id,created_at').eq('user_id',state.session.user.id).order('created_at',{ascending:false}).limit(100);if(error)throw error;const ids=(data||[]).map(x=>x.thread_id);let rows=[];if(ids.length){const{data:t,error:te}=await db.from('forum_threads').select('id,title,category_id,author_id,created_at,last_post_at,is_pinned,is_locked,moderation_status').in('id',ids).eq('moderation_status','visible');if(te)throw te;const order=new Map(ids.map((id,i)=>[id,i]));rows=(await hydrateThreads(t||[])).sort((a,b)=>(order.get(a.id)||0)-(order.get(b.id)||0))}$('#forumContent').innerHTML=pageHead('ACCOUNT','My Bookmarks','Discussions you saved for quick return.',newDiscussionButton())+`<section class="panel"><div class="panel-title">SAVED DISCUSSIONS</div><div class="thread-list">${rows.length?rows.map(threadRow).join(''):'<div class="empty-state">You have no bookmarked discussions yet.</div>'}</div></section>`}
async function renderMyDiscussions(){activeTab('');crumbs([{label:'Forum',href:'/forum/'},{label:'My Discussions'}]);if(!requireProfile())return;const{data,error}=await db.from('forum_threads').select('id,title,category_id,author_id,created_at,last_post_at,is_pinned,is_locked,moderation_status').eq('author_id',state.profile.id).order('last_post_at',{ascending:false}).limit(100);if(error)throw error;const rows=await hydrateThreads(data||[]);$('#forumContent').innerHTML=pageHead('ACCOUNT','My Discussions','Threads you started.',newDiscussionButton())+`<section class="panel"><div class="thread-list">${rows.length?rows.map(threadRow).join(''):'<div class="empty-state">You have not started a discussion yet.</div>'}</div></section>`}
async function renderMyPosts(){activeTab('');crumbs([{label:'Forum',href:'/forum/'},{label:'My Posts'}]);if(!requireProfile())return;const{data,error}=await db.from('forum_posts').select('id,thread_id,body,created_at,updated_at,moderation_status').eq('author_id',state.profile.id).order('created_at',{ascending:false}).limit(100);if(error)throw error;const ids=[...new Set((data||[]).map(p=>p.thread_id))];let tm=new Map();if(ids.length){const{data:t,error:te}=await db.from('forum_threads').select('id,title,category_id').in('id',ids);if(te)throw te;tm=new Map((t||[]).map(x=>[x.id,x]))}const cm=new Map(state.categories.map(c=>[String(c.id),c]));$('#forumContent').innerHTML=pageHead('ACCOUNT','My Posts','Your most recent forum posts.')+`<section class="panel"><div class="search-results">${(data||[]).length?(data||[]).map(p=>{const t=tm.get(p.thread_id);return`<article class="search-result"><h3><a data-route href="${urlFor({thread:p.thread_id})}#post-${p.id}">${esc(t?.title||'Discussion')}</a></h3><div class="search-excerpt">${esc(clip(p.body))}</div><div class="search-meta">${esc(cm.get(String(t?.category_id))?.name||'Forum')} · ${esc(C.fmt(p.created_at))}${new Date(p.updated_at)-new Date(p.created_at)>1000?' · EDITED':''}</div></article>`}).join(''):'<div class="empty-state">You have not posted yet.</div>'}</div></section>`}
window.ForumViews={renderHome,renderBoards,renderLatest,renderCategory,renderSearch,runSearch,renderBookmarks,renderMyDiscussions,renderMyPosts};
})();

const fs=require('fs');const path=require('path');module.exports=(req,res)=>{try{const app=fs.readFileSync(path.join(process.cwd(),'forum','forum-app.js'),'utf8');new Function(app);const html=fs.readFileSync(path.join(process.cwd(),'auth','reset','index.html'),'utf8');const blocks=[...html.matchAll(/<script(?:\s[^>]*)?>([\s\S]*?)<\/script>/gi)].map(m=>m[1]).filter(x=>x.trim());for(const src of blocks)new Function(src);res.status(200).json({ok:true,forumApp:true,resetInlineBlocks:blocks.length,forgotPassword:app.includes('FORGOT PASSWORD?'),resetPasswordCall:app.includes('resetPasswordForEmail'),resetPage:html.includes('CHOOSE A NEW PASSWORD')})}catch(e){res.status(500).json({ok:false,error:String(e&&e.message||e)})}};